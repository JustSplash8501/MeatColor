## R CMD check results

0 errors | 0 warnings | 2 notes

* This is a new release.

* HTML validation was skipped because the Apple-provided HTML Tidy available
  on the local macOS check host is too old. Package documentation, examples,
  tests, vignettes, and the PDF manual otherwise build and check successfully.

## Downstream dependencies

There are currently no downstream dependencies for this package.
