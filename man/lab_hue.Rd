% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_metrics.R
\name{lab_hue}
\alias{lab_hue}
\title{Calculate CIELAB hue angle}
\usage{
lab_hue(a, b, degrees = TRUE)
}
\arguments{
\item{a}{Numeric vector of a* coordinates.}

\item{b}{Numeric vector of b* coordinates.}

\item{degrees}{Logical. If \code{TRUE} (the default), return degrees from 0
(inclusive) to 360 (exclusive). If \code{FALSE}, return radians from 0
(inclusive) to 2*pi (exclusive).}
}
\value{
A numeric vector of CIELAB hue angles. Missing coordinates and
neutral colors produce missing results.
}
\description{
Calculates the CIELAB hue angle (hab) from a* and b* coordinates using
\code{atan2()}, with angles normalized to one complete positive rotation. Hue is
undefined when both coordinates are zero, so neutral colors return \code{NA}.
}
\details{
In degrees, 0 corresponds to the positive a* (red) axis, 90 to positive b*
(yellow), 180 to negative a* (green), and 270 to negative b* (blue).
}
\examples{
lab_hue(a = c(1, 0, -1, 0), b = c(0, 1, 0, -1))
lab_hue(a = 1, b = 1, degrees = FALSE)

}
