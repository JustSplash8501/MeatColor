% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_metrics.R
\name{add_lab_metrics}
\alias{add_lab_metrics}
\title{Add CIELAB hue and chroma columns to a data frame}
\usage{
add_lab_metrics(
  data,
  a_col = NULL,
  b_col = NULL,
  chroma_col = "chroma",
  hue_col = "hue",
  degrees = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble containing a* and b* coordinates.}

\item{a_col, b_col}{Unquoted or quoted names of the a* and b* columns. When
\code{NULL}, the defaults, columns named \code{a} and \code{b} are used.}

\item{chroma_col, hue_col}{Names for the new output columns. Defaults to
\code{"chroma"} and \code{"hue"}.}

\item{degrees}{Logical. Passed to \code{\link[=lab_hue]{lab_hue()}}; \code{TRUE} returns degrees and
\code{FALSE} returns radians.}
}
\value{
\code{data} with numeric chroma and hue columns appended. Existing
columns are preserved.
}
\description{
Adds row-wise CIELAB chroma and hue-angle calculations to a data frame. The
function works as a dplyr-style pipeline verb and preserves data-frame,
tibble, and grouping classes.
}
\examples{
measurements <- data.frame(
  treatment = c("Control", "Treatment"),
  a = c(18, 12),
  b = c(12, 8)
)

add_lab_metrics(measurements)
add_lab_metrics(measurements, a_col = "a", b_col = "b")

}
