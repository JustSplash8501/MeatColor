% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance_matrix.R
\name{plot_treatment_distances}
\alias{plot_treatment_distances}
\title{Plot average CIEDE2000 distance between treatments}
\usage{
plot_treatment_distances(
  x,
  treatment,
  metadata = NULL,
  sample_id = NULL,
  na_rm = TRUE,
  show_values = TRUE,
  digits = 1,
  low = "white",
  high = "#8B0000",
  na_color = "grey90",
  tile_color = "white",
  title = "Mean CIEDE2000 Distance Between Treatments",
  fill_label = "Mean Delta E 00"
)
}
\arguments{
\item{x}{A \code{meatcolor_distances} object from \code{\link[=lab_distances]{lab_distances()}} or a symmetric
numeric matrix with matching sample row and column names.}

\item{treatment}{Unquoted or quoted treatment column in the metadata.}

\item{metadata}{An optional data frame containing one row per sample. It is
required when \code{x} is a plain matrix and otherwise defaults to the metadata
stored in \code{x}.}

\item{sample_id}{Optional unquoted or quoted sample-identifier column in
\code{metadata}. It is required for a plain matrix and otherwise defaults to the
identifier column stored in \code{x}.}

\item{na_rm}{Logical. If \code{TRUE}, missing distances are removed when calculating
means and standard deviations.}

\item{show_values}{Logical. If \code{TRUE}, print mean distances in the tiles.}

\item{digits}{Non-negative integer number of decimal places used in labels.}

\item{low, high}{Colors used for low and high mean distances.}

\item{na_color}{Fill color for treatment combinations without usable pairs.}

\item{tile_color}{Border color for heatmap tiles.}

\item{title}{Plot title.}

\item{fill_label}{Fill-legend title.}
}
\value{
A ggplot object. Its data contain the symmetric treatment matrix and
pair counts used in the plot.
}
\description{
Produces a symmetric heatmap of mean within- and between-treatment distances
from a \code{meatcolor_distances} object or a matrix plus sample metadata.
}
\examples{
samples <- data.frame(
  sample = paste0("S", 1:4),
  treatment = rep(c("control", "treated"), each = 2),
  l = c(45, 46, 50, 51),
  a = c(15, 14, 11, 10),
  b = c(10, 11, 8, 9)
)
distances <- lab_distances(samples, sample_id = sample)

plot_treatment_distances(
  distances,
  treatment = treatment
)

# The S3 method provides the shortest interface.
plot(distances, treatment = treatment)

}
