% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myoglobin_reference.R
\name{myoglobin_ref}
\alias{myoglobin_ref}
\title{Estimate myoglobin forms using calibrated K/S references}
\usage{
myoglobin_ref(
  data,
  omb_reference,
  dmb_reference,
  mmb_reference,
  r470_col = "R470",
  r480_col = "R480",
  r520_col = "R520",
  r530_col = "R530",
  r570_col = "R570",
  r580_col = "R580",
  r610_col = "R610",
  reflectance_scale = c("percent", "proportion"),
  reference_summary = c("mean", "median"),
  bounds = c("warn", "none", "clamp"),
  keep_intermediates = FALSE
)
}
\arguments{
\item{data}{A data frame containing sample MiniScan spectral reflectance.}

\item{omb_reference, dmb_reference, mmb_reference}{Data frames containing
reflectance scans of experimentally prepared 100 percent OMb, DMb, and
MMb reference samples. Each data frame may contain one or more replicate
scans and must use the same wavelength column names as \code{data}.}

\item{r470_col, r480_col, r520_col, r530_col, r570_col, r580_col, r610_col}{Names
of the columns containing reflectance at the indicated wavelengths.}

\item{reflectance_scale}{Input scale shared by the samples and references.
Use \code{"percent"} for values such as \code{35.2}, or \code{"proportion"} for values
such as \code{0.352}. The scale is never inferred automatically. Non-missing
values must be greater than zero and no greater than 100 for \code{"percent"}
or 1 for \code{"proportion"}.}

\item{reference_summary}{How replicate reference ratios are summarized.
The default is \code{"mean"}; \code{"median"} is available for a robust summary.}

\item{bounds}{How estimates outside 0 to 100 percent are handled. \code{"warn"}
retains the estimates and emits a warning, \code{"none"} retains them without
a warning, and \code{"clamp"} restricts each estimate to the interval from 0
to 100. The \code{myoglobin_valid} column always describes the estimates before
clamping.}

\item{keep_intermediates}{Logical. If \code{TRUE}, append interpolated
reflectance, K/S values, and sample K/S ratios.}
}
\value{
\code{data} with \code{omb_pct}, \code{dmb_pct}, \code{mmb_pct},
\code{myoglobin_sum_pct}, and \code{myoglobin_valid} appended. The three estimates
are calibrated independently and therefore are not forced to total 100.
Rows with missing required reflectance return missing estimates. The
summarized calibration values are attached as the
\code{"myoglobin_reference_ratios"} attribute regardless of
\code{keep_intermediates}.
}
\description{
Estimates relative oxymyoglobin (OMb), deoxymyoglobin (DMb), and
metmyoglobin (MMb) percentages with the calibrated K/S isobestic-wavelength
method described in the AMSA Meat Color Measurement Guidelines. Separate
experimentally prepared reference spectra representing 100 percent OMb,
DMb, and MMb are required.
}
\details{
MiniScan reflectance at 474, 525, and 572 nm is linearly interpolated from
the instrument's 10 nm output before conversion to K/S. Reflectance at
610 nm is used directly. K/S is calculated as \code{(1 - R)^2 / (2 * R)}, where
\code{R} is reflectance expressed as a proportion.

The calibrated ratios are K/S474 divided by K/S525 for DMb, K/S572 divided
by K/S525 for MMb, and K/S610 divided by K/S525 for OMb. Reference values
should be prepared from the same product and experiment and measured using
the same instrument, standardization, and packaging-film conditions as the
samples.

This calibrated K/S procedure is distinct from the Krzywicki selected-
wavelength reflex-attenuance method implemented by \code{\link[=myoglobin_int]{myoglobin_int()}}.
}
\examples{
# One sample and one prepared spectrum for each reference form.
sample_scan <- data.frame(
  R470 = 20, R480 = 20, R520 = 25, R530 = 25,
  R570 = 30, R580 = 30, R610 = 35
)
omb_ref <- transform(sample_scan, R470 = 18, R480 = 18, R610 = 45)
dmb_ref <- transform(sample_scan, R470 = 30, R480 = 30, R570 = 22, R580 = 22)
mmb_ref <- transform(sample_scan, R570 = 40, R580 = 40, R610 = 22)

myoglobin_ref(
  sample_scan,
  omb_reference = omb_ref,
  dmb_reference = dmb_ref,
  mmb_reference = mmb_ref,
  reflectance_scale = "percent",
  bounds = "none"
)

}
\references{
American Meat Science Association. (2012). \emph{Meat Color Measurement
Guidelines}. Champaign, Illinois, USA.
\url{https://meatscience.org/docs/default-source/publications-resources/hot-topics/2012_12_meat_clr_guide.pdf}

King, D. A., et al. (2023). American Meat Science Association Guidelines for
Meat Color Measurement. \emph{Meat and Muscle Biology}, 6(4), 1-81.
\doi{10.22175/mmb.12473}
}
